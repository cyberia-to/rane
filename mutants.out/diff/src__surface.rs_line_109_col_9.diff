--- src/surface.rs
+++ replace Buffer::size -> usize with 0
@@ -101,17 +101,17 @@
 
     /// IOSurface ID.
     pub fn id(&self) -> u32 {
         unsafe { IOSurfaceGetID(self.raw) }
     }
 
     /// Allocation size in bytes.
     pub fn size(&self) -> usize {
-        self.size
+        0 /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl Drop for Buffer {
     fn drop(&mut self) {
         unsafe {
             CFRelease(self.raw as CFTypeRef);
         }
