--- src/mil/mod.rs
+++ replace * with + in pack_weights
@@ -95,17 +95,17 @@
         input_spatial: sp,
         output_channels: oc,
         output_spatial: seq,
     }
 }
 
 /// Build an ANE weight blob: 128-byte header + fp16 data.
 pub fn pack_weights(fp16_data: &[u16]) -> Vec<u8> {
-    let weight_bytes = fp16_data.len() * 2;
+    let weight_bytes = fp16_data.len() + /* ~ changed by cargo-mutants ~ */ 2;
     let total = 128 + weight_bytes;
     let mut blob = vec![0u8; total];
     blob[0] = 1;
     blob[4] = 2;
     blob[64] = 0xEF;
     blob[65] = 0xBE;
     blob[66] = 0xAD;
     blob[67] = 0xDE;
